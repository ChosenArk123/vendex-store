<script src="https://cdn.tailwindcss.com"></script>
<script>
  tailwind.config = {
    theme: {
      extend: {
        fontFamily: {
          sans: ['system-ui','-apple-system','BlinkMacSystemFont','Segoe UI','Roboto','sans-serif']
        },
        colors: {
          background: 'var(--background)',
          foreground: 'var(--foreground)',
          card: 'var(--card)',
          'card-foreground': 'var(--card-foreground)',
          primary: 'var(--primary)',
          'primary-foreground': 'var(--primary-foreground)',
          'primary-container': 'var(--primary-container)',
          'on-primary-container': 'var(--on-primary-container)',
          'secondary-container': 'var(--secondary-container)',
          tertiary: 'var(--tertiary)',
          destructive: 'var(--destructive)',
          'muted-foreground': 'var(--muted-foreground)',
          border: 'var(--border)',
        },
        borderRadius: { xl:'1rem','2xl':'1.75rem','3xl':'2.5rem' },
        // MERGED: Added Elastic Physics
        transitionTimingFunction: { 
          emphasized: 'cubic-bezier(0.2,0,0,1)',
          elastic: 'cubic-bezier(0.34, 1.56, 0.64, 1)' 
        },
        keyframes: {
          'slide-up-fade': {
            '0%': { opacity:'0', transform:'translateY(24px) scale(0.96)' },
            '100%': { opacity:'1', transform:'translateY(0) scale(1)' }
          },
          'pop': { '0%': { transform:'scale(0.92)' }, '100%': { transform:'scale(1)' } },
          'blob': {
            '0%': { transform:'translate(0px,0px) scale(1)' },
            '33%': { transform:'translate(40px,-60px) scale(1.1)' },
            '66%': { transform:'translate(-30px,30px) scale(0.95)' },
            '100%': { transform:'translate(0px,0px) scale(1)' }
          }
        },
        animation: {
          enter: 'slide-up-fade 0.7s cubic-bezier(0.2,0,0,1) forwards',
          pop: 'pop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1) forwards',
          blob: 'blob 12s infinite alternate cubic-bezier(0.4,0,0.2,1)'
        }
      }
    }
  };
</script>

<style>
  html { scroll-behavior: smooth; }

  /* PURE LIGHT MODE */
  :root {
    --background: #FDF7FF;
    --foreground: #1D1B20;
    
    --card: rgba(255, 255, 255, 0.85); 
    --card-foreground: #1D1B20;

    --primary: #5B3FD6;
    --primary-foreground: #FFFFFF;

    --primary-container: #E5DEFF;
    --on-primary-container: #1E0A5F;

    --secondary-container: #EEE6FF;
    --tertiary: #B44574;

    --destructive: #BA1A1A;
    --muted-foreground: #514B57;
    
    --border: rgba(203, 195, 214, 0.6);
    
    --dot: rgba(20, 10, 40, 0.08);
    --ambientA: color-mix(in oklab, var(--primary) 12%, transparent);
    --ambientB: color-mix(in oklab, var(--tertiary) 10%, transparent);
    --dotOpacity: 0.3;
    --dotBlend: normal;
    
    /* Global Physics Bezier */
    --e-emphasized: cubic-bezier(0.2,0,0,1);
    --e-elastic: cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  /* GLOBAL TRANSITIONS */
  body, .ui-card, .btn, .chip, input, select, textarea,
  h1, h2, h3, h4, h5, h6, p, span, div, a, li, button,
  svg, path, circle {
    transition-property: background-color, border-color, color, fill, stroke, transform, box-shadow;
    transition-timing-function: var(--e-emphasized);
    transition-duration: 0.5s;
  }

  .no-scrollbar::-webkit-scrollbar{ display:none; }
  .no-scrollbar{ scrollbar-width:none; -ms-overflow-style:none; }

  /* CARD STYLES */
  .ui-card {
    background-color: var(--card);
    border: 1px solid var(--border);
    box-shadow: 0 12px 32px rgba(0,0,0,0.08);
    border-radius: 2.5rem; /* MERGED: Rounded-3xl consistency */
    position: relative;
    overflow: hidden;
  }

  /* Elastic Hover Effect for Cards */
  .ui-card:hover {
    transform: translateY(-4px) scale(1.005);
    box-shadow: 0 20px 48px rgba(0,0,0,0.12);
    transition-timing-function: var(--e-elastic);
  }

  .ui-card::after {
    content:"";
    position:absolute;
    inset:-2px;
    pointer-events:none;
    opacity:0;
    transition: opacity 260ms var(--e-emphasized);
    background: radial-gradient(900px 260px at 20% 0%,
      color-mix(in oklab, var(--primary) 20%, transparent),
      transparent 60%);
  }
  .ui-card:hover::after{ opacity:1; }


  /* ---------- COMPONENT CLASSES ---------- */
  
  /* CHIP (Badge/Tag) */
  .chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.5rem 1.25rem; /* MERGED: Slightly larger padding for pill shape */
    font-size: 0.875rem; 
    font-weight: 800; /* MERGED: Bolder text */
    border-radius: 9999px;
    border: 1px solid transparent;
    transition: all 0.4s var(--e-elastic); /* MERGED: Elastic physics */
    white-space: nowrap;
    cursor: default;
  }

  .chip:hover {
    transform: translateY(-3px) scale(1.08); /* MERGED: Bouncier hover */
    background: color-mix(in oklab, var(--secondary-container) 46%, transparent);
    border-color: color-mix(in oklab, var(--primary) 45%, var(--border));
    box-shadow: 0 14px 35px rgba(0,0,0,0.18);
  }

  .chip:active {
    transform: translateY(0px) scale(0.96);
    box-shadow: 0 8px 18px rgba(0,0,0,0.14);
  }

  /* BUTTONS */
  .btn {
    border-radius: 9999px;
    padding: 0.75rem 1.5rem;
    position: relative;
    overflow: hidden;
    font-weight: 800;
    /* MERGED: Elastic physics for buttons */
    transition: transform 300ms var(--e-elastic), 
                box-shadow 300ms var(--e-elastic), 
                background-color 220ms var(--e-emphasized), 
                color 220ms var(--e-emphasized);
  }

  .btn-hero, .btn-primary {
    background: var(--primary);
    color: var(--primary-foreground);
    box-shadow: 0 4px 10px rgba(0,0,0,0.15);
  }
  .btn-hero:hover, .btn-primary:hover {
    background: color-mix(in oklab, var(--primary) 90%, white);
    box-shadow: 0 8px 20px rgba(0,0,0,0.25);
  }

  .btn:hover { 
    transform: translateY(-3px) scale(1.04); /* MERGED: Bouncier hover */
    box-shadow: 0 14px 32px rgba(0,0,0,0.18); 
  }
  
  .btn:active { 
    transform: translateY(0) scale(0.96); 
    box-shadow: 0 8px 18px rgba(0,0,0,0.14); 
  }

  .btn-soft {
    background: color-mix(in oklab, var(--secondary-container) 65%, transparent);
    color: var(--foreground);
    border: 1px solid color-mix(in oklab, var(--border) 60%, transparent);
  }
  .btn-soft:hover { background: color-mix(in oklab, var(--secondary-container) 85%, transparent); }

  .btn-ghost { border-radius: 9999px; }

  .btn:focus-visible {
    outline: none;
    box-shadow: 0 0 0 4px color-mix(in oklab, var(--primary) 25%, transparent), 0 16px 40px rgba(0,0,0,0.18);
  }
</style>