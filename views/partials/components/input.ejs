<%
  const cls = `ui-input ${className || ''}`;
%>
<input
  class="<%= cls %>"
  type="<%= type || 'text' %>"
  name="<%= name %>"
  id="<%= id || name %>"
  value="<%= typeof value !== 'undefined' ? value : '' %>"
  placeholder="<%= placeholder || '' %>"
  <%= required ? 'required' : '' %>
/>
